def append(x,y):
   return (x + element for element in y)
   
def generate_bits(n):
  if n == 0: return [];
  if n == 1: return ["0","1"]:
  else:
    return (append("0",generate_bits(n-1))+append("1", generate_bits(n-1))
    
print (generate_bits(3))

//python_code
